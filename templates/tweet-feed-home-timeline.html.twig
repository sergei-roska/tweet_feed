{#<p>{{ dump(tweet.created_at) }}</p>#}

<ul class="tp--list">
  <li class="tp--item">
    <div class="tp--item__date">{{ tweet.created_at|raw }}</div>
      <div class="tp--item__tweet">
        <span class="tp--item__user">{{ tweet.user.name }}</span>
        {{ tweet.text }}
      </div>
    <div>More information at:<span class="tp--item__more">{{ tweet.source|raw }}</span></div>
  </li>
</ul>
  <button class="tp--button">View all</button>
